server:
  port: ${PORT:8080}

spring:
  application:
    name: fileserver
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/fileserver
    username: tr33p
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1